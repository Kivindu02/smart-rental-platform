apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service-db
  namespace: smart-rental
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service-db
  template:
    metadata:
      labels:
        app: auth-service-db
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "db"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef: { name: app-secrets, key: AUTH_DB_USER }
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef: { name: app-secrets, key: AUTH_DB_PASSWORD }
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service-db
  namespace: smart-rental
spec:
  selector:
    app: auth-service-db
  ports:
    - port: 5432
      targetPort: 5432
